# SmartFill Server

Simple Express + MongoDB server.

## Dev

```bash
npm install
cp .env.example .env
npm run dev
```

## Routes

- `GET /api/health` — Service health
- `GET /api/example` — Demo route
